# `manuscript/output/` — rendered PDFs

The compiled paper. Commit the final PDF here so reviewers and collaborators can read it without compiling LaTeX themselves.

`.aux`, `.log`, `.bbl` and other LaTeX intermediates are gitignored — only the `.pdf` is tracked.
